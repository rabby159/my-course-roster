/* eslint-disable react/prop-types */

const Cart = ({cart}) => {
    return (
        <div className="pl-5">
                <li className="">{cart.title}</li>
        </div>
    );
};

export default Cart;